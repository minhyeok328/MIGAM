---
title: "미감 P0 테스트 계획"
status: DRAFT
version: "0.3.5"
last_updated: "2026-09-05"
authoritative_for:
  - "P0 계층별 테스트 전략과 검증 범위"
  - "데이터·추천·백엔드·프론트·E2E·브라우저·접근성 테스트 시나리오"
  - "P0 품질 게이트와 검증 증거 규칙"
related_documents:
  - "../00-governance/decision-register.md"
  - "../01-product/prd-p0.md"
  - "../04-ux/user-flows.md"
  - "../04-ux/screen-spec.md"
  - "../05-engineering/api-guidelines.md"
  - "../05-engineering/security-privacy.md"
  - "../05-engineering/system-architecture.md"
  - "./acceptance-criteria.md"
  - "./traceability-matrix.md"
---

# 미감 P0 테스트 계획

## 1. 목적과 현재 상태

이 문서는 P0를 데이터 입력부터 사용자 판단까지 반복 검증하는 방법을 정의한다. 현재 Django 데이터·서비스·API 테스트는 `backend/`에서 `uv run python manage.py test ../tests --verbosity 1`, 프론트 단위·컴포넌트 테스트는 `frontend/`에서 `npm test`로 실행한다. OpenAPI 생성 타입·Zod 경계와 격리 데모 API 테스트도 존재한다. 테스트 수와 통과 결과는 실행 날짜별 작업 패킷에 기록하며 고정된 현재 수치로 복제하지 않는다. 자동 브라우저 E2E 러너는 후속 범위다.

검증 기술의 방향은 다음과 같이 확정되어 있다.

- 데이터 파이프라인·Django 서비스·DRF API의 단위·통합 테스트
- Vitest와 React Testing Library 기반 프론트 단위·컴포넌트 테스트
- OpenAPI 계약과 프론트 경계 검증
- Playwright 기반 사용자 E2E
- Chromium 전체 E2E, Firefox·WebKit 핵심 스모크
- 자동 접근성 검사와 키보드·스크린리더·확대·모션 감소 수동 검증

## 2. 테스트 원칙

1. **불변식을 먼저 검증한다.** 하드 필터, 전시 상태, 추천 이유, 이미지 권리 위반은 평균 점수로 상쇄할 수 없다.
2. **외부 서비스 없이 재현한다.** 핵심 검증은 고정 픽스처와 대표 데모 데이터로 실행하고 외부 API·지도 실패도 주입한다.
3. **경계에서 계약을 검증한다.** 원본 데이터→정규화, 서비스→API, API→프론트 어댑터, 브라우저 저장소 버전 경계를 각각 검사한다.
4. **성공·빈·오류·저하 상태를 함께 다룬다.** 정상 경로만 통과해도 완료로 보지 않는다.
5. **사용자 행동을 취향 신호와 분리한다.** 조회·검색·비교·지도·최근 본 전시가 추천을 바꾸지 않는지 회귀 검증한다.
6. **테스트가 데이터 권리를 약화하지 않는다.** 테스트 이미지도 재사용 허용 자산 또는 저장소에서 만든 명시적 테스트 자산만 사용한다.
7. **개발 이벤트를 전송·보존하지 않는다.** 허용 이름·속성 밖 값, 외부 네트워크·SDK와 서버·브라우저 영속은 검증 실패다.

## 3. 테스트 데이터

### 3.1 결정적 소형 픽스처

단위·통합 테스트는 각 규칙의 경계값을 분리한 작은 고정 데이터를 사용한다.

- 전시 상태: 현재, 예정, 종료, 공식 취소, 충돌 후보
- 최소 품질: 전시명·시작일·종료일·장소·지역·유효 상태·공식 상세 URL·공식 출처를 각각 하나씩 누락·무효 처리한 불합격 레코드와 모든 핵심 항목을 갖춘 합격 레코드
- 기관 후보 심사: 5/5·4/5·3/5 `CORE_PASS`, 4/5이지만 출처 템플릿·이전 이력·추가 공식 페이지에서 같은 필수 필드 누락이 재현된 사례, 정책·robots·인증·CAPTCHA·호출 제한으로 반복 수집이 불가능한 사례와 `PASS`·`HOLD`
- 기관 lifecycle·health: `CANDIDATE`·`PROVISIONAL`·`ACTIVE`·`SUSPENDED`, `HEALTHY`·`DEGRADED`, 14일 미만·이상, UTC 날짜는 다르지만 `Asia/Seoul` 날짜는 같은 실행과 그 반대 경계, 승격 3회 연속 `SUCCESS`, 서로 다른 IngestionRun의 운영 1회·2회 연속 최종 `FAILED`, 중간 `SUCCESS`, 같은 실행의 재시도 후 성공, 핵심 대상 최종 미수집, 선택 대상 실패, 핵심 미완성값 정본 반영 시도, `PROVISIONAL` 실패의 counter 0, Source 정상·중단, 미해결 구조 충돌
- 수집 문제: `POLICY_BLOCK`, `ACCESS_BLOCK`, `STRUCTURAL_CRITICAL`, `STRUCTURAL_OPTIONAL`, `RECORD_EXCEPTION`, 실행 중·실행 밖 Critical, `ENTRY`·`SOURCE` scope, 공유 Source에서 한 기관만 영향받는 사례와 Source 전체 영향 사례, 미해결 Critical 수집 전 차단
- 의미 있는 변경: 새 전시, 종료일 변경·연장, 요금·장소·예약 방식 변경, 전시 취소, 공식 설명에서 P0 승인·버전 고정 정규화 규칙이 만든 Canonical 필드 변경의 SourceRecord→정규화→Canonical→ChangeHistory 허용 사례와, 전시명·시작일·지역·일반 상태 전이, 임의의 목록 밖 Canonical 필드, footer·배너·tracking parameter·HTML whitespace·필드 순서·수집 시각·재시도 횟수·semantic 결과가 같은 raw hash의 거부 사례
- 최신성: `FRESH`, `STALE`, `UNVERIFIED`, 수집 실패
- 날짜: 시작·종료 경계, 하루 겹침, 일부 겹침, 공식 휴관일
- 지역: 시·도 전체, 시·군·구, 좌표 있음·없음
- 요금: 무료, 성인 기본권, 여러 연령 가격, 할인·얼리버드·패키지, 미확인
- 예약·예상 관람시간: 공식, `UNKNOWN`; 작품 수·영상 길이·유사 전시 기반 추론 시도
- 접근성·감각: 충족, 충돌, 미확인
- 이미지: `REUSE_ALLOWED`, `LINK_ONLY`, `RIGHTS_UNKNOWN`, 없음, 로딩 실패
- 출처: 필드별 우선순위, 동일 값, 충돌, 공식 페이지 삭제
- 중복: 강한 자동 병합, 모호한 후보, 순회전
- 추천: 취향 없음, 테스트만, 관심 1~2개, 충분한 명시 신호, 종료·미확인 후보 혼입

### 3.2 대표 E2E 데이터

E2E 데이터는 서울·경기·인천을 중심으로 일부 비수도권을 포함하고, 전시·작품·기관·상태·매체·가격·권리·접근성 예외를 모두 재현해야 한다. 데이터 크기가 확대되어도 같은 시나리오 식별자는 유지한다.

### 3.3 데모 데이터 규모 목표

P0 대표 데모 데이터는 전시 300~500, 작품 500~1,000, 기관 약 100을 목표로 한다. 이 수치는 화면 성능과 탐색 품질을 검증하기 위한 목표이며 테스트 성공을 위해 외부 출처에서 권리 불명확 자료를 채워 넣는 근거가 아니다.

## 4. 계층별 테스트 카탈로그

### 4.1 데이터 파이프라인

| ID | 시나리오 | 핵심 판정 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-001` | 정규화 규칙 | 날짜·주소·지역·요금·매체·분류가 고정 기대값으로 변환되고 원본이 보존되며, 근거 없는 행사 형식은 제목으로 추정하지 않고 `형식 미확인`으로 남음 | `AC-004`, `AC-017` |
| `TEST-002` | 전시 상태·최신성 경계 | 현재·예정·종료·공식 취소와 `FRESH`·`STALE`·`UNVERIFIED`가 48시간·3일 경계와 확인 이력에 맞게 판정되고, 최소 품질을 통과한 재확인 실패만 제한 검색되며 핵심 항목 충돌은 `EXCLUDED`로 격리되고, 실패가 마지막 검증 정본을 덮어쓰지 않음 | `AC-016`, `AC-017`, `AC-023`, `AC-024` |
| `TEST-003` | 중복 병합·순회전 | 강한 근거만 자동 병합, 모호한 것은 후보, 순회전은 별도 레코드 유지 | `AC-017` |
| `TEST-004` | 필드별 출처 충돌 | 운영정보와 표준정보의 우선순위, 원본·표시값·충돌·이력이 역추적됨 | `AC-016`, `AC-017` |
| `TEST-005` | 미디어 권리 필터 | 재사용 허용만 노출 가능하고 링크 전용·미확인은 복제·캐시·노출 상태가 되지 않음 | `AC-015`, `AC-017` |

### 4.2 추천 품질

| ID | 시나리오 | 핵심 판정 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-006` | 하드 필터 불변식 | 지역·날짜·최대 예산·필수 접근성·회피 감각과 필수로 지정한 예약·예상 관람시간 위반 또는 `UNKNOWN` 통과 추천 0건 | `AC-005`, `AC-024` |
| `TEST-007` | 미확인 정보 정책 | 사용자가 필수로 지정한 요금·예약·예상 관람시간·접근성·감각의 `UNKNOWN`은 조건 미충족, 선호 모드와 매체·분위기·공간 유형·행사 형식의 `UNKNOWN`은 중립, 비필수 방문 정보는 추가 확인으로 구분 | `AC-005`, `AC-016`, `AC-024` |
| `TEST-008` | 취향 신호 의미 | 테스트·관심만 개인화에 기여하고 조회·검색·비교·지도·최근 본 기록은 점수를 바꾸지 않음 | `AC-003`, `AC-011` |
| `TEST-009` | 설명·중복·다양성·탐색 | 이유 불일치·동일 전시 중복 0건, 과도한 기관·매체 집중 제한, 연결된 탐색 후보 확인, 기본·추천 정렬이 앞선 후보 조건을 깨지 않음 | `AC-007`, `AC-008` |

추천 평가 픽스처는 입력 신호, 하드 필터 통과 후보, 특징 기여, 재정렬 전후 결과와 최종 이유를 함께 보존해 실패 원인을 재현할 수 있어야 한다.

### 4.3 백엔드·API

| ID | 시나리오 | 핵심 판정 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-010` | OpenAPI 계약 | 실제 요청·응답과 계약의 필수값·열거형·오류가 일치하고 비호환 변경이 감지됨 | `AC-018` |
| `TEST-011` | 검색·필터·정렬·페이지 | 상태 기본값, 키워드 전체 상태, 관련도 우선, 상태별 정렬 사용 가능 여부·전환 안내, 예약·예상 관람시간의 required/preferred URL 복원, 24개 묶음, 총수·다음 여부·중복 없음 | `AC-006`, `AC-007` |
| `TEST-012` | 추천·상세 서비스 | 추천 불변식, 이유, 가격·예약·시간·출처·종료·미확인 응답이 도메인 규칙과 일치 | `AC-005`, `AC-008`, `AC-009`, `AC-010` |

서비스 단위 테스트는 수집기를 직접 호출하지 않고 정규화된 도메인과 고정 저장소를 사용한다. API 통합 테스트는 인증이 없는 사용자 API와 운영자 전용 API의 접근 경계를 함께 검증한다.

### 4.4 프론트엔드

| ID | 시나리오 | 핵심 판정 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-013` | 브라우저 저장소 | 취향·관심·최근 본 전시 유지, 버전 마이그레이션, 손상값 회복, 부분·전체 초기화 | `AC-011`, `AC-014` |
| `TEST-014` | 탐색 상태·핵심 컴포넌트 | URL 복원, 필터 칩, 더 보기, 관심, 비교 제한, Dialog·Sheet/Drawer·Popover·Select·Tabs·Tooltip·Accordion·Checkbox의 이름·역할·키보드·포커스 상태 | `AC-006`, `AC-007`, `AC-011`, `AC-012`, `AC-018` |
| `TEST-015` | 공통 화면 상태 | 로딩·빈·부분 오류·전체 오류·종료·`STALE`·최소 품질을 통과한 `UNVERIFIED`·텍스트형 대체 카드와 핵심 게이트 미달 사용자 미노출 | `AC-015`, `AC-016`, `AC-018`, `AC-024` |

프론트 경계 검증은 API 응답이 스키마와 맞지 않을 때 잘못된 값을 정상 화면으로 전달하지 않고 해당 영역의 안전한 오류 상태로 전환하는지 포함한다.

### 4.5 Playwright E2E

| ID | 흐름 | 주요 검증 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-016` | 홈 → 취향 테스트 → 추천 결과 | 적응형 응답, 정성 결과, 근거, 현재·예정 후보 | `AC-001`~`AC-003`, `AC-008` |
| `TEST-017` | 지금 갈 전시 찾기 → 조건 입력 → 추천 | 지역·날짜, 분위기 최대 3개와 설명, 행사 형식 미확인, 선택 조건, 하드 필터, 0건 회복 | `AC-004`~`AC-006` |
| `TEST-018` | 전시 탐색 → 검색·필터 → 상세 | 기본·상태별 정렬, URL, 24개, 출처·방문 정보 | `AC-007`, `AC-009` |
| `TEST-019` | 전시 2~3개 → 비교 | 최대 3개, 반응형 표현, 종료·미확인 구분 | `AC-012` |
| `TEST-020` | 전시 관심 → 관심 목록 → 해제 | 브라우저 유지, 자동 분류, 긍정 신호 제거 | `AC-011` |
| `TEST-021` | 작품 관심 → 관심 목록 | 작품 메타데이터, 관심 저장, 관련 탐색 | `AC-010`, `AC-011` |
| `TEST-022` | 기관 관심 → 관심 기관 | 기관 방문 정보, 현재·예정 연결, 자동 분류 | `AC-010`, `AC-011` |
| `TEST-023` | 검색 → 현재·예정·종료 전시 | 키워드 전체 상태와 종료 경고·현재 우선 | `AC-007`, `AC-009`, `AC-016` |
| `TEST-024` | 목록 → 지도 보기 → 목록 복귀 | 같은 필터·결과, 핀 상세, 위치 거절 회복 | `AC-013` |
| `TEST-025` | 브라우저 데이터 부분·전체 초기화 | 삭제 범위, 확인·취소, 즉시 화면 갱신 | `AC-014` |
| `TEST-026` | 새로고침 후 취향·관심 유지 | 저장값 유지, URL 복원, 서버 프로필 부재 | `AC-003`, `AC-011`, `AC-014` |
| `TEST-027` | 권리 없는 이미지 → 텍스트형 카드 | 이미지 네트워크 요청 없음, 동일 정보·행동 | `AC-015` |

### 4.6 브라우저·디바이스

| ID | 범위 | 주요 검증 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-028` | Chromium 전체 E2E | `TEST-016`~`TEST-027` 전부와 데스크톱·모바일 뷰포트 | `AC-019`, `AC-020` |
| `TEST-029` | Firefox·WebKit 핵심 스모크 | 홈, 전시 검색, 상세, 관심·브라우저 저장, 취향 테스트 | `AC-019`, `AC-020` |
| `TEST-030` | 최신 주요 실제 브라우저 확인 | Chrome·Edge·Firefox·Safari, Android Chrome·iOS Safari에서 핵심 과업 | `AC-020` |

`최신`은 검증 시점의 현재 안정 버전을 뜻하며 보고서에 실제 브라우저·OS 버전을 남긴다. 구형 IE나 오래된 모바일 브라우저는 P0 검증 대상이 아니다.

### 4.7 접근성

| ID | 범위 | 주요 검증 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-031` | 자동 접근성 검사 | 핵심 화면의 이름·역할·값, 레이블, 대체 텍스트, 대비, 중복 ID 등 자동 검출 항목 | `AC-020` |
| `TEST-032` | 키보드·포커스 | 모든 핵심 과업, 보이는 포커스, 논리 순서, 대화상자·바텀시트·팝오버 진입·복귀 | `AC-020` |
| `TEST-033` | 스크린리더·상태 전달 | 관심, 현재·예정·종료, 오류, 로딩, 필터 변화, 비교 표·카드의 의미 | `AC-020` |
| `TEST-034` | 확대·터치·모션 감소 | 확대 시 핵심 정보 손실 없음, 터치 오작동 없음, 감소 설정에서 비필수 모션 제거 | `AC-020` |

### 4.8 관리자·회복성

| ID | 범위 | 주요 검증 | 연결 AC |
| --- | --- | --- | --- |
| `TEST-035` | 운영자 인증·품질 화면 | 비인증·비staff 차단, staff의 `/admin/data-status/` 접근, 전체·현재·예정·종료·`STALE`·`UNVERIFIED`·충돌·중복·권리·수집 성공·실패·재확인·기관별 CORE_PASS·`PASS`·`HOLD`와 보류 사유·네 lifecycle·health·연속 최종 실패 수·우선 재검증·CollectionIssue·격리 레코드·전이 근거·검증 경과일·연속 성공일·의미 변경 chain·마지막 실행·Source 상태·미해결 구조 충돌 및 Django Admin drill-through 확인 | `AC-021`, `AC-025`~`AC-027` |
| `TEST-036` | 외부 출처·지도 장애 | 마지막 검증 데이터 보존, 부분 오류, 목록 대체, 재시도, 잘못된 새 사실 없음 | `AC-013`, `AC-016` |
| `TEST-037` | 비범위 표면 감사 | 사용자 경로·화면·네트워크 계약·영속 데이터에서 `P0-OUT-001`~`P0-OUT-008` 기능이 노출되지 않음 | `AC-022` |
| `TEST-038` | 개발 전용 이벤트 계약 | 허용 이벤트·속성·값 범위만 수용하고 민감·식별 값을 거절하며, 외부 요청·분석 SDK·서버 DB·`localStorage` 영속은 0건이고 운영 빌드에서는 no-op | `AC-014` |
| `TEST-039` | 동기화 명령·재확인 실행 경로 | 네 변경 명령과 배포 스케줄러가 대상 선택 뒤 공통 서비스를 사용하고 `PROVISIONAL`·`ACTIVE` 모두 같은 레코드 게이트와 정본·파생·게시 경로를 사용하는지 확인한다. 공유 Source에서도 InstitutionRunResult·health를 기관별로 기록하고, 승격 증거는 `PROVISIONAL`, 연속 실패 수는 `ACTIVE`에서만 갱신해야 한다. `--source`는 미등록·비정상 Source와 `CANDIDATE`·`SUSPENDED`·미해결 Critical scope를 네트워크 전에 거부하되 Critical 없는 `DEGRADED`는 실행 가능하고 우선 재검증해야 한다. `show_refresh_schedule`은 due·우선 재검증·Critical scope·승격 진행 상태를 읽기 전용으로 사용한다. source·canonical ID 범위, 48시간·3일·종료 제외 경계, 실패 시 정본·이력 보존과 기관별 `SourceConflict` 연결도 검증한다. | `AC-016`, `AC-017`, `AC-023`, `AC-026`, `AC-027` |
| `TEST-040` | 전시 데이터 최소 품질 게이트 | 핵심 항목을 하나씩 누락·무효·충돌시킨 레코드는 `CORE_PASS`가 아니며 격리되고, 핵심 항목을 갖춘 채 요금·예약·예상 관람시간·접근성·감각만 `UNKNOWN`인 레코드는 `CORE_PASS`이며 추론값이 0건이고, 같은 `UNKNOWN`을 필수 방문 조건으로 지정하면 추천 조건을 통과하지 않음 | `AC-005`, `AC-017`, `AC-024` |
| `TEST-041` | 기관 후보 allowlist 심사 | 5/5·4/5는 비율 조건 통과, 3/5는 `HOLD` 후 `CANDIDATE` 유지, 4/5 이상이어도 출처 템플릿·이전 이력·추가 공식 페이지의 구조적 반복 누락 또는 반복 수집을 막는 정책·접근 제한이면 `HOLD` 후 진행하지 않음, 통과 기관은 `PROVISIONAL`, `HOLD`는 lifecycle 상태가 아님 | `AC-021`, `AC-025` |
| `TEST-042` | 기관 lifecycle·서비스 적격성·`ACTIVE` 승격 | `CANDIDATE → PROVISIONAL → ACTIVE → SUSPENDED → PROVISIONAL`의 온보딩·복구 흐름과 `PROVISIONAL`·`ACTIVE` CORE_PASS의 동일 서비스 경로를 확인한다. `promotion_validation_started_at + 14일`과 `finished_at`의 `Asia/Seoul` 달력일로 서로 다른 날짜 3회 연속 최종 `SUCCESS`·중간 `FAILED` 0을 검증하며 UTC·서울 날짜 경계도 포함한다. retry 후 모든 핵심 대상을 처리한 성공은 허용하되 `PROVISIONAL`의 핵심 대상 최종 미수집·기관별 최종 `FAILED`·핵심 미완성값 Canonical commit·실행 중 Critical은 InstitutionRunResult·InstitutionQualificationRun의 최종 `FAILED`와 streak 초기화로 판정한다. 공유 IngestionRun이 다른 기관 때문에 실패해도 해당 기관 결과가 성공이면 QualificationRun을 실패로 바꾸지 않는다. 선택 대상 실패와 단건 격리는 승격 실패로 오인하지 않는다. 사용자 승인 의미 변경 allowlist·denylist, SourceRecord→승인 정규화 규칙과 버전→Canonical→ChangeHistory chain, 마지막 성공·Source 정상·구조적 누락·정책/접근 문제·미해결 구조 충돌 0, `SUSPENDED → PROVISIONAL` 재검증 초기화를 확인한다. | `AC-021`, `AC-023`, `AC-026`, `AC-027` |
| `TEST-043` | 기관 health·실패 사다리·Critical·격리 | 같은 실행의 503·여러 retry 후 핵심 대상 성공은 `SUCCESS`이고 실패 수가 증가하지 않는다. 첫 최종 `FAILED`는 `ACTIVE + DEGRADED + count 1 + 우선 재검증`, 다음 성공은 count 0과 원인별 health 복구, 중간 성공 없는 서로 다른 IngestionRun의 두 번째 최종 `FAILED`는 날짜와 무관하게 `SUSPENDED`인지 확인한다. `POLICY_BLOCK`·`ACCESS_BLOCK`·`STRUCTURAL_CRITICAL` 각각은 1회 확인으로 즉시 중단되고 추가 요청·우회·핵심 반영이 0건이어야 한다. 실행 중 Critical은 영향 InstitutionRunResult와 전체 IngestionRun을 `FAILED`로 만들고 `ACTIVE` counter를 0→1 또는 1→2로 올리되 즉시 중단하며, 실행 밖 Critical은 가상 실행·실패 수를 만들지 않아야 한다. `STRUCTURAL_OPTIONAL`은 실행 `SUCCESS`, `UNKNOWN`·이미지 대체, lifecycle 유지 + `DEGRADED`, `ACTIVE` count 불변이며, `RECORD_EXCEPTION`은 해당 레코드만 정본·파생·승격 증거에서 격리하고 기관 상태를 유지한다. 반복 단건 패턴의 Critical 재분류, `ENTRY` 기본 scope와 Source 전체 근거가 있을 때만 `SOURCE`로 확대되는지 검증한다. `PROVISIONAL`의 일반 실패·실행 중 Critical은 health `DEGRADED`, count 0, QualificationRun `FAILED`, streak 초기화이고 미해결 Critical은 모든 변경 명령을 네트워크 전에 차단해야 한다. `PROVISIONAL` 선택 구조는 `SUCCESS + DEGRADED`, 단건 예외는 health 불변이다. `ACTIVE` 런타임 결과가 InstitutionQualificationRun·과거 PromotionEvidence를 생성·변경하지 않고, `SUSPENDED`만으로 마지막 정상 정본이 일괄 `EXCLUDED`되지 않는지도 확인한다. | `AC-021`, `AC-023`, `AC-024`, `AC-027` |

`TP-001`은 `TEST-039` 중 registry 부트스트랩, `PROVISIONAL`·`ACTIVE`·`DEGRADED` 허용, `CANDIDATE`·`SUSPENDED`·비정상 Source·ENTRY/SOURCE Critical 수집 전 차단과 성공 확정 전 재검사, 세 변경 명령의 기관별 결과와 성공 확정 원자성을 구현했다. `TEST-043` 중 같은 실행 중복 결과 방지, `ACTIVE` 첫·두 번째 실패와 성공 복구, 열린 Critical을 반영한 즉시 중단·전이 근거, `PROVISIONAL` counter 0, 선택 구조 issue가 열린 동안 `DEGRADED` 유지도 구현했다. `TP-002`는 `TEST-042` 중 명시적 자격 실행, 승인 표본 처리 완료와 정상 단건 격리, Canonical ChangeHistory allowlist, 14일 미만·정확한 경계·서울 자정·같은 날짜 중복, 마지막 실패 이후 연속 성공, 의미 변경·Source·Critical·충돌 veto와 PromotionEvidence 승격을 구현했다. `TP-003`은 `TEST-005`의 권리 상태·처리 허용·현재 이력과 안전한 인라인 판정, `TEST-040`의 선택 정보 `UNKNOWN`·확인된 부정 분리·핵심 적격성 독립을 저장 계층에서 구현했다. `TP-004`는 `TEST-010`·`TEST-011` 중 전시·기관 FTS5 검색과 내부 GET 계약을 구현했다. `TP-005`는 `TEST-006`~`TEST-009`, `TEST-012` 중 현재·예정 추천 후보 게이트, 필수조건·`UNKNOWN`, 명시 신호, 결정적 점수·다양성·연결 탐색·실제 이유와 내부 POST 계약을 구현했다. 배포 스케줄러·복구 승인·실행 중 Critical 자동 분류·기관별 재시도 telemetry·선택 필드와 추천 특성의 실제 수집기 변환·OperatingSchedule·Admin·상세/비교 API와 해당 화면은 후속 범위다. 현재 홈·검색·추천 프론트 구현과 검증은 아래 TP-006 항목을 따른다.

## 5. 실행 환경

### TP-006 구현 범위

`frontend/src/shared/api/client.test.ts`는 출처·날짜·URL·권리·계약 오류, `forms.test.ts`는 예산 0·방문 조건·최대 3개 분위기·draft/적용 분리, `frontend/src/app/App.test.tsx`는 경로 분리·홈 API 0회·film 대체·검색/추천·더 보기·늦은 응답·오류/0건·이미지 대체를 검증한다. `frontend/src/features/discovery/SearchPanel.test.tsx`는 기본 목록·접힌 필터·적용/취소·지역 종속값 초기화·미제출 검색어 보존을 확인한다. `frontend/src/app/SiteShell.test.tsx`는 홈 헤더 축소와 탐색·모바일의 고정 높이 조건, `frontend/src/features/home/useHomeReveal.test.tsx`는 모션 감소·관찰 API 대체·정리를 검증한다. `frontend/src/test/dev-log.test.ts`는 Vite 프록시 query 제거, `contract.typecheck.ts`는 OpenAPI 관람시간 입력의 생성 타입을 검증한다. `tests/discovery/test_demo_api.py`는 가상 seed 보호와 실제 검색·추천 서비스의 필수/UNKNOWN/0건 결과를 검증한다.

TP-006은 `TEST-017`·`TEST-018`·`TEST-027`의 일부를 컴포넌트 수준에서 검증한 것이며 자동 브라우저 E2E 통과가 아니다. Chrome의 1440px·390px에서 홈·탐색·필터와 키보드·모션 감소를 한정적으로 확인했다. 전체 P0 E2E, Firefox·WebKit·실제 모바일·200% 확대·스크린리더 검수는 남아 있다. 키 없는 로컬 실행은 [Frontend README](../../frontend/README.md), 실행 시점·수치·제외 사항은 [TP-006](../07-execution/task-packets/TP-006-frontend-discovery.md)에 기록한다.

### 5.1 기본 모드

- 단위·통합·컴포넌트·E2E는 외부 네트워크 호출을 기본 차단하고 고정 픽스처를 사용한다.
- 개발 이벤트 검증은 네트워크·분석 SDK·서버 DB·브라우저 영속을 모두 차단한 상태에서 수행하고, 운영 빌드에서는 no-op을 확인한다.
- 시간·현재 날짜·위치·지도 응답을 제어할 수 있어야 한다.
- 각 테스트는 브라우저 저장값, 데이터베이스, 캐시와 비교 선택을 독립적으로 초기화한다.
- 무작위 다양성 재정렬은 시드 또는 고정 결과로 재현 가능해야 한다.

### 5.2 통합 재현 모드

네이티브 개발 환경과 컨테이너 통합 환경에서 동일한 핵심 픽스처 결과가 나와야 한다. 컨테이너 검증은 외부 API 키 없이 프론트·백엔드·데모 데이터가 함께 동작하는지 확인한다.

### 5.3 외부 출처 계약 확인

실제 공공 API와 기관 수집기 검증은 승인된 출처별로 별도 수행한다. 이 검증은 호출 제한과 이용조건을 지키고, 실패가 핵심 오프라인 테스트 결과를 불안정하게 만들지 않도록 자동 게이트와 분리한다.

## 6. 브라우저 검증 범위

### 전체 자동화

Chromium에서 `TEST-016`~`TEST-027` 전체, 접근성 자동 검사, 데스크톱·모바일 반응형 핵심 상태를 수행한다.

### 스모크 자동화

Firefox와 WebKit에서는 다음을 최소 수행한다.

- 홈 두 경로 진입
- 전시 검색·필터·상세
- 전시 관심과 새로고침 후 브라우저 저장 유지
- 취향 테스트 핵심 흐름
- 종료·권리 없는 이미지 상태

### 실제 환경 확인

최신 Edge·Safari, Android Chrome·iOS Safari에서는 터치, 바텀시트, 하단 내비게이션, 확대, 위치 거절과 목록 복귀를 포함한 핵심 과업을 확인한다.

## 7. 접근성 검증 방식

자동 검사만으로 합격시키지 않는다.

1. 핵심 화면의 자동 접근성 검사
2. 키보드만으로 `TEST-016`~`TEST-025`의 대표 행동 수행
3. 대화상자·바텀시트·팝오버·드롭다운의 포커스 수동 확인
4. 스크린리더로 상태·관심·필터·오류·비교 구조 확인
5. 브라우저 확대에서 홈, 탐색, 상세, 비교, 데이터 삭제 수행
6. 모션 감소 설정에서 비필수 효과 제거 확인
7. 이미지 비표시 상태에서 대체 텍스트와 텍스트형 카드 정보 동등성 확인

자동 도구의 오탐을 제외할 때는 검사 위치, 이유, 수동 확인 결과를 증거에 남긴다. 핵심 과업을 막는 미해결 접근성 문제는 합격으로 처리하지 않는다.

## 8. 품질 게이트

### 변경 단위 게이트

- 변경한 계층의 단위·계약·컴포넌트 테스트 통과
- 연결된 `AC-###`와 추적성 매트릭스 동기화
- 무관용 불변식 위반 없음
- 권리·개인정보·접근성 회귀 없음

### P0 후보 게이트

- `TEST-001`~`TEST-043` 중 구현 범위에 해당하는 필수 시나리오 통과
- `TEST-016`~`TEST-027` Chromium 전체 통과
- Firefox·WebKit 스모크 통과
- 최신 주요 브라우저 핵심 과업 확인
- 접근성 자동·수동 검증 완료
- 외부 출처가 없어도 대표 데모 데이터로 재현
- 실패 테스트를 재시도만으로 숨기지 않으며 불안정 테스트가 남아 있지 않음

## 9. 결함 우선순위

| 우선순위 | 예시 | P0 판정 |
| --- | --- | --- |
| 차단 | 무관용 불변식 위반, 데이터 삭제 오류, 일반 사용자의 관리자 접근, 핵심 흐름 완료 불가 | 해결 전 불합격 |
| 높음 | 주요 브라우저에서 핵심 행동 실패, 키보드·스크린리더 핵심 흐름 차단, 검색 상태 소실 | 해결 전 불합격 |
| 중간 | 대체 행동은 있으나 일부 정보·상태가 부정확하거나 불편 | 영향 평가 후 원칙적으로 해결 |
| 낮음 | 의미를 바꾸지 않는 시각·문구 불일치 | 기록 후 우선순위 결정 |

## 10. 검증 증거

각 테스트 실행은 다음을 남긴다.

- 실행 시각과 코드·문서 버전
- 테스트 데이터 또는 픽스처 버전
- 실행 환경, 브라우저·OS 버전
- 통과·실패·제외 수와 실패 원인
- E2E 실패 시 스크린샷·트레이스 등 재현 자료
- 접근성 수동 검토자, 보조기술·설정과 결과
- 외부 출처 계약 확인 시 출처와 확인 시점

동기화 관리 명령의 이름·범위는 `AC-023`의 확정 계약을 따른다. 현재 Django 검증 명령은 `uv run --project backend python backend/manage.py test tests --verbosity 1`이며, 실행 결과의 발견·통과·실패 수를 변경 증거에 기록한다.

## 11. 열린 결정

- `OD-003`: `RESOLVED`. 출처 계약 픽스처는 [`source-qualification.json`](../../fixtures/source-qualification.json)이며 3개 Source·5개 기관·24개 `PASS`·1개 격리를 검증한다. 실제 허용 필드와 호출 제약은 [`sources.yaml`](../../sources.yaml)을 따른다.
- `OD-004`: DEC-101로 확정한 마루 부리·SUIT는 라이선스·로딩 실패·한국어 렌더링 검증을 현재 적용하고 `TEST-030`·`TEST-034`에 연결한다. 최종 워드마크·로고 형식과 해당 자산의 시각 승인은 남아 있다.
